<form method="post" action="{{ path('app_legal_page_delete', {'slug': legal_page.slug}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce document légal ? Cette action est irréversible.?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ legal_page.id) }}">
    <button class="btn-principal">Supprimer</button>
</form>
