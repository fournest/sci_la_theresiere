<form method="post" action="{{ path('app_tarif_delete', {'id': tarif.id}) }}" onsubmit="return confirm('Êtes-vous sûr(e) de vouloir supprimer)?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tarif.id) }}">
    <button class="btn-principal">Supprimer</button>
</form>
