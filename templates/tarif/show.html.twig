{% extends 'base.html.twig' %}

{% block title %}Tarif{% endblock %}

{% block body %}
    <h1>Tarif</h1>

    <table class="table">
        <tbody>
            
            <tr>
                <th>Prix de la réservation</th>
                <td>{{ tarif.prixReservation }}</td>
            </tr>
            <tr>
                <th>Prix de l'option</th>
                <td>{{ tarif.prixOption }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn-principal" href="{{ path('app_tarif_index') }}">Retour à la liste</a>

    <a class="btn-principal" href="{{ path('app_tarif_edit', {'id': tarif.id}) }}">Modifier</a>

    {{ include('tarif/_delete_form.html.twig') }}
{% endblock %}
