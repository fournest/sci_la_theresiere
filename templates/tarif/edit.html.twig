{% extends 'base.html.twig' %}

{% block title %}Modifier le tarif{% endblock %}

{% block body %}
    <h1>Modifiez le tarif</h1>

    {{ include('tarif/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a class="btn-principal" href="{{ path('app_tarif_index') }}">Retour à la liste</a>

    {{ include('tarif/_delete_form.html.twig') }}
{% endblock %}
