{% extends 'base.html.twig' %}

{% block title %}Visite{% endblock %}

{% block body %}
    <h1>Visite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ visite.id }}</td>
            </tr>
            <tr>
                <th>DateVisite</th>
                <td>{{ visite.dateVisite ? visite.dateVisite|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>UtilisateurId</th>
                <td>{{ visite.utilisateurId }}</td>
            </tr>
            <tr>
                <th>DateResaSouhaite</th>
                <td>{{ visite.dateResaSouhaite ? visite.dateResaSouhaite|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_visite_index') }}">Retour à la lsite</a>

    <a href="{{ path('app_visite_edit', {'id': visite.id}) }}">Modifier</a>

    {{ include('visite/_delete_form.html.twig') }}
{% endblock %}
