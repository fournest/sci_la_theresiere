{% extends 'base.html.twig' %}

{% block title %}Options{% endblock %}

{% block body %}
    <h1>Options</h1>

    <table class="table">
        <tbody>
            <tr>
                {# <th>Id</th>
                <td>{{ option.id }}</td> #}
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ option.nom }}</td>
            </tr>
        </tbody>
    </table>

    <button><a href="{{ path('app_option_index') }}">Retour à la liste</a></button>

    <button><a href="{{ path('app_option_edit', {'id': option.id}) }}">Modifier</a></button>

    {{ include('option/_delete_form.html.twig') }}
{% endblock %}
