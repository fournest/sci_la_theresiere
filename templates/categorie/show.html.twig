{% extends 'base.html.twig' %}

{% block title %}Catégories{% endblock %}

{% block body %}
    <h1>Catégories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorie.nom }}</td>
            </tr>
        </tbody>
    </table>

    <button><a href="{{ path('app_categorie_index') }}">Retour à la liste</a></button>

    <button><a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}">Modifier</a></button>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
